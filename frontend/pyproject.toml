[project]
name = "lease-reviewer-frontend"
version = "0.1.0"
description = "Streamlit frontend for the lease reviewer demo"
requires-python = ">=3.11"

dependencies = [
    "streamlit>=1.41.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
